#include "traverse.h"
//helper value

void BFSTraverse(Board start , Board goal,int level , Direction formerDirecrtion){
    // start.disp();
    // std::cout << level << std::endl;
    // if(start.getTable()!=goal.getTable() && level <5){
    //     if(start.moveEmptyTile(Direction::UP) && formerDirecrtion!=Direction::DOWN){
    //         BFSTraverse(start,goal,++level,Direction::UP);
    //     }
    //     if(start.moveEmptyTile(Direction::DOWN) && formerDirecrtion != Direction::UP){
    //         BFSTraverse(start,goal,++level,Direction::DOWN);
    //     }
    //     if(start.moveEmptyTile(Direction::LEFT) && formerDirecrtion != Direction::RIGHT){
    //         BFSTraverse(start,goal,++level,Direction::LEFT);
    //     }
    //     if(start.moveEmptyTile(Direction::RIGHT) && formerDirecrtion != Direction::LEFT){
    //         BFSTraverse(start,goal,++level,Direction::LEFT);
    //     }else {
    //         // start.disp();
    //         // std::cout << "^___^ I found it\n"<<std::endl;
    //         return;
    //     }
    // }else{
    //     std::cout << "______________________________________________\n";
    //     return;
    // }
    
}